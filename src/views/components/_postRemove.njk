{% extends "./_modal.njk" %}
{% set modalId = "remove" %}
{% block content %}
    <div>
        Você deseja mesmo remover essa postagem?
        <div class="flex-row justify-end">
            <button class='text-blue-500 font-semibold p-2 hover:bg-black/10 rounded-lg'>Cancelar</button>
            <form action="/post/{{post.id}}/delete" method="post">
                <input type="submit" value="Remover" class="cursor-pointer text-blue-500 font-semibold p-2 hover:bg-black/10 rounded-lg"></input>
            </form>
        </div>
    </div>
{% endblock %}