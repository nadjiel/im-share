{% extends "../layout.njk" %}

{% set title = "Usuário" %}

{% block content %}
  <section class="flex-row items-center justify-center gap-4 m-4">
    <img 
      width="100"
      height="100"
      class="rounded-full"
      src='{{user.picture}}'/>
    <div class="w-full max-w-md">
      <h1 class="text-3xl">{{user.name}}</h1>
      <h2 class="text-xl">@{{user.username}}</h2>
    </div>
  </section>
  <div class="flex-col items-center">
    <section class="grid gap-2 gap-y-4 md:gap-y-2 md:grid-cols-3 justify-center">
      {% for post in user.posts %}
        {% include "../components/_postItem.njk" %}
      {% endfor %}
      {% for comment in user.comments %}
        {% include "../components/_commentAt.njk" %}
      {% endfor %}
    </section>
  </div>
{% endblock %}

{% block script %}{% endblock %}